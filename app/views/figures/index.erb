<table style="width:100%">
  <tr>
    <th> This formatting is terrible, but don't have time to make it pretty</th>
    <th>Figure</th>
    <th>Titles</th>
    <th>Landmarks</th>
  </tr>

    <% Figure.all.each do |figure| %>
    <tr>
      <td> <%= figure.name %> <td>


        <% figure.titles.each do |title| %>
          <td><%= title.name %></td>
        <% end %>


          <% figure.landmarks.each do |landmark| %>
          <td> <%= landmark.name %> </td>
        <% end %>
      </tr>
    <% end %>

<table>
